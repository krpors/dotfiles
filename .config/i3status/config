general {
	output_format = "i3bar"
	colors = true
	color_degraded = "#ffffff"
	color_good = "#00aa00"
	color_bad = "#aa0000"
	interval = 5
}

load {
	format = "[ load: %1min, %5min, %15min ]"
}

ethernet _first_ {
}

disk "/" {
	format = "/ = %percentage_used used"
}

order += "load"
order += "disk /"
order += "ethernet _first_"
